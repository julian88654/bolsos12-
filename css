:root {
  --accent:#c76c53;
  --muted:#6b6b6b;
  --bg:#f7f6f4;
  --card:#ffffff;
  --glass: rgba(255,255,255,0.7);
  font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
}
* { box-sizing:border-box }
html,body { height:100%; margin:0; background:var(--bg); color:#222 }

/* Header */
.header { display:flex; align-items:center; justify-content:space-between; padding:18px 36px; background:linear-gradient(90deg,#fff 0%,#fff0 60%); position:sticky; top:0; z-index:30; border-bottom:1px solid rgba(0,0,0,0.04) }
.brand { display:flex; gap:14px; align-items:center }
.logo { width:48px; height:48px; border-radius:12px; background:linear-gradient(135deg,var(--accent),#ffb88c); display:flex; align-items:center; justify-content:center; color:white; font-weight:700 }
.brand h1 { font-size:18px; margin:0 }
.brand p { margin:0; font-size:12px; color:var(--muted) }
.search { flex:1; max-width:720px; margin:0 24px }
.search input { width:100%; padding:10px 14px; border-radius:10px; border:1px solid rgba(0,0,0,0.08); background:white }
.actions { display:flex; gap:10px; align-items:center }
.btn { padding:8px 12px; border-radius:10px; border:0; background:var(--card); box-shadow:0 1px 4px rgba(0,0,0,0.04); cursor:pointer }

/* Layout */
.container { max-width:1200px; margin:28px auto; padding:0 20px }
.hero { display:grid; grid-template-columns:1fr 360px; gap:18px; margin-bottom:20px }
.hero-card { background:linear-gradient(180deg,var(--card),#fff); border-radius:16px; padding:20px; box-shadow:0 6px 20px rgba(23,23,23,0.05) }
.hero-card h2 { margin:0 0 6px 0 }
.hero-card p { margin:0; color:var(--muted) }
.hero-image { height:240px; border-radius:12px; background-image:linear-gradient(135deg,rgba(199,108,83,0.12),rgba(0,0,0,0.02)); display:flex; align-items:center; justify-content:center; color:var(--accent); font-weight:700 }

/* Sidebar */
.sidebar { background:var(--card); padding:16px; border-radius:12px; height:100% }
.filters h3 { margin:6px 0 12px }
.filter-group { margin-bottom:12px }
.chip { display:inline-block; padding:6px 10px; border-radius:999px; border:1px solid rgba(0,0,0,0.06); margin:4px 6px 4px 0; cursor:pointer }

/* Grid */
.grid-controls { display:flex; justify-content:space-between; align-items:center; margin:12px 0 }
.product-grid { display:grid; grid-template-columns:repeat(4,1fr); gap:16px }
.card { background:var(--card); border-radius:12px; padding:12px; box-shadow:0 6px 18px rgba(0,0,0,0.04); display:flex; flex-direction:column }
.thumb { height:220px; border-radius:10px; background-size:cover; background-position:center; margin-bottom:10px; position:relative; overflow:hidden }
.thumb .fav { position:absolute; right:10px; top:10px; background:rgba(255,255,255,0.9); border-radius:8px; padding:6px }
.product-title { font-weight:600; margin:0 0 6px }
.price { font-weight:700 }
.meta { font-size:12px; color:var(--muted); margin-top:8px }
.thumbnails { display:flex; gap:8px; flex-wrap:wrap; margin-top:8px }
.thumbnails img { width:46px; height:46px; border-radius:8px; object-fit:cover; cursor:pointer; border:2px solid transparent }
.thumbnails img.selected { border-color:var(--accent) }

/* Modal */
.modal-backdrop { position:fixed; inset:0; background:rgba(0,0,0,0.5); display:none; align-items:center; justify-content:center; z-index:60 }
.modal { background:var(--card); border-radius:12px; padding:18px; max-width:1000px; width:94%; display:grid; grid-template-columns:1fr 260px; gap:12px }
.modal .big { height:560px; border-radius:10px; background-size:cover; background-position:center }
.modal .info { overflow:auto }
.modal .close { margin-left:auto }

/* Footer */
footer { padding:22px; text-align:center; color:var(--muted); font-size:13px }

/* Responsive */
@media (max-width:1100px) { .product-grid{grid-template-columns:repeat(3,1fr)} .hero{grid-template-columns:1fr 300px} }
@media (max-width:880px) { .product-grid{grid-template-columns:repeat(2,1fr)} .hero{grid-template-columns:1fr} .search{max-width:none; margin:0 12px} }
@media (max-width:560px) { .product-grid{grid-template-columns:1fr} .brand p{display:none} .header{padding:12px} .hero-image{height:180px} }
